import { Get, Post, Body, Param, Delete, Put } from '@nestjs/common';
import { GenericController } from 'src/common/decorators/controller.decorator';
import { {{pascalCase name}}Service } from './{{name}}.service';
import { {{pascalCase name}} } from '../../entities/{{name}}.entity';

@GenericController('{{name}}')
export class {{pascalCase name}}Controller {
constructor(private readonly {{camelCase name}}Service: {{pascalCase name}}Service) {}

@Get()
  async findAll(): Promise<{{pascalCase name}}[]> {
  return await this.{{camelCase name}}Service.findAll();
  }

  @Get(':id')
  async findOne(@Param('id') id: number): Promise<{{pascalCase name}}> {
    return await this.{{camelCase name}}Service.findOne(id);
  }

  @Post()
  async create(@Body() data: Partial<{{pascalCase name}}>): Promise<{{pascalCase name}}> {
      return await this.{{camelCase name}}Service.create(data);
  }

  @Put(':id')
  async update(@Param('id') id: number, @Body() data: Partial<{{pascalCase name}}>): Promise<{{pascalCase name}}> {
      return await this.{{camelCase name}}Service.update(id, data);
  }

  @Delete(':id')
  async remove(@Param('id') id: number): Promise<void> {
    return await this.{{camelCase name}}Service.remove(id);
  }
}