import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { PageDto } from 'src/common/dto/page.dto';
import { {{pascalCase name}} } from '../../entities/{{name}}.entity';

@Injectable()
export class {{pascalCase name}}Service {
constructor(
@InjectRepository({{pascalCase name}})
private repository: Repository<{{pascalCase name}}>,
  ) {}

  async findAll(): Promise<PageDto<{{pascalCase name}}>> {
    return await this.repository.find();
  }

  async findOne(id: number): Promise<{{pascalCase name}}> {
    return await this.repository.findOne({ where: { id } });
  } 

  async create(dto: {{pascalCase name}}Dto): Promise<{{pascalCase name}}> {
    const entity = this.repository.create(dto);
    return await this.repository.save(entity);
  }

  async update(id: number, dto: {{pascalCase name}}Dto): Promise<{{pascalCase name}}> {
    await this.repository.update(id, dto);
    return await this.findOne(id);
  }

  async remove(id: number): Promise<void> {
    await this.repository.delete(id);
  }
}